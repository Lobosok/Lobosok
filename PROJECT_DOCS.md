# Документация проекта HR Telegram Bot

## 🎯 Цель проекта
Создание Telegram-бота для автоматизации первичного этапа найма сотрудников. Бот позволяет HR-менеджерам публиковать вакансии, а соискателям — просматривать их и оставлять отклики (анкеты).

## 🛠 Технологический стек
- **Язык:** Python 3.12
- **Фреймворк бота:** `python-telegram-bot` (v21.9)
- **База данных:** SQLite
- **ORM:** SQLAlchemy
- **Конфигурация:** `python-dotenv`

## 📂 Структура проекта
```
HRBOT/
├── bot/
│   ├── bot.py          # Основная логика бота, хендлеры, клавиатуры
│   └── __init__.py
├── db/
│   ├── models.py       # Описание таблиц БД (Vacancy, Candidate)
│   └── database.sqlite3 # Файл базы данных (создается автоматически)
├── core/               # (Зарезервировано) Для сложной бизнес-логики
├── main.py             # Точка входа (запуск БД и бота)
├── config.py           # Загрузка настроек
├── .env                # Токены и секреты (не попадает в репозиторий)
└── requirements.txt    # Зависимости
```

## ⚙️ Архитектура и логика работы

### 1. База данных (SQLite)
Используется две основные таблицы:
- **vacancies**: Хранит список открытых позиций (`id`, `title`, `description`).
- **candidates**: Хранит отклики (`id`, `name`, `phone`, `resume`).

### 2. Логика бота (`bot.py`)
- **ConversationHandler**: Реализует пошаговую анкету кандидата (Имя -> Телефон -> Резюме).
- **InlineButtons**: Интерактивное меню для выбора вакансий и подачи заявки.
- **Admin Protection**: Декоратор `@admin_only` ограничивает доступ к командам управления (`/add_vacancy`, `/list_candidates`) только для указанного `ADMIN_ID`.

### 3. Сценарии использования
- **HR-менеджер:**
  - Добавляет вакансии через команду `/add_vacancy`.
  - Просматривает список всех откликов через `/list_candidates`.
- **Кандидат:**
  - Заходит в бота, видит список вакансий.
  - Нажимает "Откликнуться".
  - Бот последовательно запрашивает данные.
  - Данные сохраняются в БД.

## 📝 Заметки для разработчика
- **Версия библиотеки:** Используется версия `21.9` библиотеки `python-telegram-bot` для стабильности (в v22+ были проблемы с инициализацией).
- **Миграции:** Сейчас структура БД создается через `Base.metadata.create_all`. При изменении моделей в будущем лучше использовать Alembic.
- **Уведомления:** Код отправки уведомлений админу написан, но закомментирован в `bot.py` (функция `get_resume`).

## 🚀 План развития (To-Do)
1. [ ] Включить уведомления администратора о новых заявках.
2. [ ] Добавить интеграцию с Google Sheets (выгрузка кандидатов).
3. [ ] Реализовать удаление вакансий.
4. [ ] Добавить возможность прикреплять файлы (PDF резюме), а не только текст.
