# HR Telegram Bot

Этот бот помогает HR-менеджерам публиковать вакансии и собирать отклики от кандидатов в Telegram.

## Функционал
- **Для HR (Администратора):**
  - Добавление вакансий.
  - Просмотр списка вакансий.
  - Просмотр списка кандидатов (откликов).
  - Уведомления о новых откликах (настраивается).
- **Для Кандидата:**
  - Просмотр доступных вакансий.
  - Подача заявки (анкета: Имя, Телефон, Резюме).

---

## Установка и запуск

### Предварительные требования
1. Установленный **Python 3.8+**.
2. Полученный токен бота от @BotFather.

### 🐧 Инструкция для Linux

1. **Распакуйте архив** с проектом.
2. Откройте терминал в папке проекта.
3. **Создайте виртуальное окружение:**
   ```bash
   python3 -m venv .venv
   ```
4. **Активируйте окружение:**
   ```bash
   source .venv/bin/activate
   ```
5. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```
6. **Настройте .env:**
   - Переименуйте `.env.example` в `.env` (если файла .env нет).
   - Откройте `.env` и вставьте ваш токен: `TELEGRAM_TOKEN=ваш_токен`.
7. **Запустите бота:**
   ```bash
   python main.py
   ```

### 🪟 Инструкция для Windows

1. **Распакуйте архив** с проектом.
2. Откройте командную строку (cmd) или PowerShell в папке проекта.
3. **Создайте виртуальное окружение:**
   ```cmd
   python -m venv .venv
   ```
4. **Активируйте окружение:**
   - В cmd:
     ```cmd
     .venv\Scripts\activate
     ```
   - В PowerShell:
     ```powershell
     .venv\Scripts\Activate.ps1
     ```
5. **Установите зависимости:**
   ```cmd
   pip install -r requirements.txt
   ```
6. **Настройте .env:**
   - Создайте файл `.env` (или используйте существующий).
   - Пропишите внутри: `TELEGRAM_TOKEN=ваш_токен`.
7. **Запустите бота:**
   ```cmd
   python main.py
   ```

---

## Команды бота

- `/start` - Запуск бота, главное меню.
- `/apply` - Подать заявку (для кандидатов).

### Команды администратора (доступны только владельцу)
- `/add_vacancy <Название>` - Добавить новую вакансию.
- `/list_vacancies` - Показать все вакансии.
- `/list_candidates` - Показать всех кандидатов.

## Структура проекта
- `main.py` - Точка входа.
- `bot/` - Логика бота и обработчики.
- `db/` - База данных (SQLite) и модели.
- `requirements.txt` - Список библиотек.
